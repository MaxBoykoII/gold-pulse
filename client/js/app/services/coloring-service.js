angular.module('GoldPulse')
    .service('ColoringService', [function(){
    let quantiles = [];
    
        this.setQuantiles = function(cleanData){
            let dates = cleanData[0].dates;
        };
    }]);